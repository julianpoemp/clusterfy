{
  "name": "node-deco-ipc",
  "version": "0.0.1",
  "description": "Decorator based IPC library for NodeJS using Typescript ",
  "main": "src/index.ts",
  "dependencies": {
    "rollup": "^3.17.3"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.0",
    "@rollup/plugin-typescript": "^11.0.0",
    "rollup-plugin-generate-package-json": "^3.2.0",
    "tslib": "^2.5.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "start": "rollup --config rollup.config_es.mjs -w",
    "build": "rm -rf ./dist && rollup --config rollup.config_es.mjs && cp LICENSE dist/LICENSE",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julian Poemp",
  "license": "MIT"
}
